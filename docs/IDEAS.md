# IDEAS - SDURI Admin Platform

## Root Discipline Cleanup

Move `app.py`, `config.py`, `requirements.txt`, `setupDatabase.sh`, `startSite.sh` from root to appropriate subdirectories (`src/` or similar). Root should contain only `.gitignore`, `README.md`, `CLAUDE.md`.
